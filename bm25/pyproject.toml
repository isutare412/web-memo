[project]
name = "webmemo-bm25"
version = "0.1.0"
description = "BM25 sparse embedding service for web-memo"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "fastembed",
]

[dependency-groups]
dev = [
    "ruff",
]
