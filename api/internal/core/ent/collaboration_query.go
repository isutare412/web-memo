// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/isutare412/web-memo/api/internal/core/ent/collaboration"
	"github.com/isutare412/web-memo/api/internal/core/ent/memo"
	"github.com/isutare412/web-memo/api/internal/core/ent/predicate"
	"github.com/isutare412/web-memo/api/internal/core/ent/user"
)

// CollaborationQuery is the builder for querying Collaboration entities.
type CollaborationQuery struct {
	config
	ctx              *QueryContext
	order            []collaboration.OrderOption
	inters           []Interceptor
	predicates       []predicate.Collaboration
	withCollaborator *UserQuery
	withMemo         *MemoQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CollaborationQuery builder.
func (cq *CollaborationQuery) Where(ps ...predicate.Collaboration) *CollaborationQuery {
	cq.predicates = append(cq.predicates, ps...)
	return cq
}

// Limit the number of records to be returned by this query.
func (cq *CollaborationQuery) Limit(limit int) *CollaborationQuery {
	cq.ctx.Limit = &limit
	return cq
}

// Offset to start from.
func (cq *CollaborationQuery) Offset(offset int) *CollaborationQuery {
	cq.ctx.Offset = &offset
	return cq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cq *CollaborationQuery) Unique(unique bool) *CollaborationQuery {
	cq.ctx.Unique = &unique
	return cq
}

// Order specifies how the records should be ordered.
func (cq *CollaborationQuery) Order(o ...collaboration.OrderOption) *CollaborationQuery {
	cq.order = append(cq.order, o...)
	return cq
}

// QueryCollaborator chains the current query on the "collaborator" edge.
func (cq *CollaborationQuery) QueryCollaborator() *UserQuery {
	query := (&UserClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(collaboration.Table, collaboration.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, collaboration.CollaboratorTable, collaboration.CollaboratorColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMemo chains the current query on the "memo" edge.
func (cq *CollaborationQuery) QueryMemo() *MemoQuery {
	query := (&MemoClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(collaboration.Table, collaboration.FieldID, selector),
			sqlgraph.To(memo.Table, memo.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, collaboration.MemoTable, collaboration.MemoColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Collaboration entity from the query.
// Returns a *NotFoundError when no Collaboration was found.
func (cq *CollaborationQuery) First(ctx context.Context) (*Collaboration, error) {
	nodes, err := cq.Limit(1).All(setContextOp(ctx, cq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{collaboration.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cq *CollaborationQuery) FirstX(ctx context.Context) *Collaboration {
	node, err := cq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Collaboration ID from the query.
// Returns a *NotFoundError when no Collaboration ID was found.
func (cq *CollaborationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cq.Limit(1).IDs(setContextOp(ctx, cq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{collaboration.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cq *CollaborationQuery) FirstIDX(ctx context.Context) int {
	id, err := cq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Collaboration entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Collaboration entity is found.
// Returns a *NotFoundError when no Collaboration entities are found.
func (cq *CollaborationQuery) Only(ctx context.Context) (*Collaboration, error) {
	nodes, err := cq.Limit(2).All(setContextOp(ctx, cq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{collaboration.Label}
	default:
		return nil, &NotSingularError{collaboration.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cq *CollaborationQuery) OnlyX(ctx context.Context) *Collaboration {
	node, err := cq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Collaboration ID in the query.
// Returns a *NotSingularError when more than one Collaboration ID is found.
// Returns a *NotFoundError when no entities are found.
func (cq *CollaborationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cq.Limit(2).IDs(setContextOp(ctx, cq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{collaboration.Label}
	default:
		err = &NotSingularError{collaboration.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cq *CollaborationQuery) OnlyIDX(ctx context.Context) int {
	id, err := cq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Collaborations.
func (cq *CollaborationQuery) All(ctx context.Context) ([]*Collaboration, error) {
	ctx = setContextOp(ctx, cq.ctx, "All")
	if err := cq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Collaboration, *CollaborationQuery]()
	return withInterceptors[[]*Collaboration](ctx, cq, qr, cq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cq *CollaborationQuery) AllX(ctx context.Context) []*Collaboration {
	nodes, err := cq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Collaboration IDs.
func (cq *CollaborationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cq.ctx.Unique == nil && cq.path != nil {
		cq.Unique(true)
	}
	ctx = setContextOp(ctx, cq.ctx, "IDs")
	if err = cq.Select(collaboration.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cq *CollaborationQuery) IDsX(ctx context.Context) []int {
	ids, err := cq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cq *CollaborationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cq.ctx, "Count")
	if err := cq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cq, querierCount[*CollaborationQuery](), cq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cq *CollaborationQuery) CountX(ctx context.Context) int {
	count, err := cq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cq *CollaborationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cq.ctx, "Exist")
	switch _, err := cq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cq *CollaborationQuery) ExistX(ctx context.Context) bool {
	exist, err := cq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CollaborationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cq *CollaborationQuery) Clone() *CollaborationQuery {
	if cq == nil {
		return nil
	}
	return &CollaborationQuery{
		config:           cq.config,
		ctx:              cq.ctx.Clone(),
		order:            append([]collaboration.OrderOption{}, cq.order...),
		inters:           append([]Interceptor{}, cq.inters...),
		predicates:       append([]predicate.Collaboration{}, cq.predicates...),
		withCollaborator: cq.withCollaborator.Clone(),
		withMemo:         cq.withMemo.Clone(),
		// clone intermediate query.
		sql:  cq.sql.Clone(),
		path: cq.path,
	}
}

// WithCollaborator tells the query-builder to eager-load the nodes that are connected to
// the "collaborator" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CollaborationQuery) WithCollaborator(opts ...func(*UserQuery)) *CollaborationQuery {
	query := (&UserClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withCollaborator = query
	return cq
}

// WithMemo tells the query-builder to eager-load the nodes that are connected to
// the "memo" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CollaborationQuery) WithMemo(opts ...func(*MemoQuery)) *CollaborationQuery {
	query := (&MemoClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withMemo = query
	return cq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Collaboration.Query().
//		GroupBy(collaboration.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cq *CollaborationQuery) GroupBy(field string, fields ...string) *CollaborationGroupBy {
	cq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CollaborationGroupBy{build: cq}
	grbuild.flds = &cq.ctx.Fields
	grbuild.label = collaboration.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//	}
//
//	client.Collaboration.Query().
//		Select(collaboration.FieldUserID).
//		Scan(ctx, &v)
func (cq *CollaborationQuery) Select(fields ...string) *CollaborationSelect {
	cq.ctx.Fields = append(cq.ctx.Fields, fields...)
	sbuild := &CollaborationSelect{CollaborationQuery: cq}
	sbuild.label = collaboration.Label
	sbuild.flds, sbuild.scan = &cq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CollaborationSelect configured with the given aggregations.
func (cq *CollaborationQuery) Aggregate(fns ...AggregateFunc) *CollaborationSelect {
	return cq.Select().Aggregate(fns...)
}

func (cq *CollaborationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cq); err != nil {
				return err
			}
		}
	}
	for _, f := range cq.ctx.Fields {
		if !collaboration.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cq.path != nil {
		prev, err := cq.path(ctx)
		if err != nil {
			return err
		}
		cq.sql = prev
	}
	return nil
}

func (cq *CollaborationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Collaboration, error) {
	var (
		nodes       = []*Collaboration{}
		_spec       = cq.querySpec()
		loadedTypes = [2]bool{
			cq.withCollaborator != nil,
			cq.withMemo != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Collaboration).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Collaboration{config: cq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cq.withCollaborator; query != nil {
		if err := cq.loadCollaborator(ctx, query, nodes, nil,
			func(n *Collaboration, e *User) { n.Edges.Collaborator = e }); err != nil {
			return nil, err
		}
	}
	if query := cq.withMemo; query != nil {
		if err := cq.loadMemo(ctx, query, nodes, nil,
			func(n *Collaboration, e *Memo) { n.Edges.Memo = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cq *CollaborationQuery) loadCollaborator(ctx context.Context, query *UserQuery, nodes []*Collaboration, init func(*Collaboration), assign func(*Collaboration, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Collaboration)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (cq *CollaborationQuery) loadMemo(ctx context.Context, query *MemoQuery, nodes []*Collaboration, init func(*Collaboration), assign func(*Collaboration, *Memo)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Collaboration)
	for i := range nodes {
		fk := nodes[i].MemoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(memo.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "memo_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (cq *CollaborationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cq.querySpec()
	_spec.Node.Columns = cq.ctx.Fields
	if len(cq.ctx.Fields) > 0 {
		_spec.Unique = cq.ctx.Unique != nil && *cq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cq.driver, _spec)
}

func (cq *CollaborationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(collaboration.Table, collaboration.Columns, sqlgraph.NewFieldSpec(collaboration.FieldID, field.TypeInt))
	_spec.From = cq.sql
	if unique := cq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cq.path != nil {
		_spec.Unique = true
	}
	if fields := cq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, collaboration.FieldID)
		for i := range fields {
			if fields[i] != collaboration.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if cq.withCollaborator != nil {
			_spec.Node.AddColumnOnce(collaboration.FieldUserID)
		}
		if cq.withMemo != nil {
			_spec.Node.AddColumnOnce(collaboration.FieldMemoID)
		}
	}
	if ps := cq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cq *CollaborationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cq.driver.Dialect())
	t1 := builder.Table(collaboration.Table)
	columns := cq.ctx.Fields
	if len(columns) == 0 {
		columns = collaboration.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cq.sql != nil {
		selector = cq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cq.ctx.Unique != nil && *cq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cq.predicates {
		p(selector)
	}
	for _, p := range cq.order {
		p(selector)
	}
	if offset := cq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CollaborationGroupBy is the group-by builder for Collaboration entities.
type CollaborationGroupBy struct {
	selector
	build *CollaborationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cgb *CollaborationGroupBy) Aggregate(fns ...AggregateFunc) *CollaborationGroupBy {
	cgb.fns = append(cgb.fns, fns...)
	return cgb
}

// Scan applies the selector query and scans the result into the given value.
func (cgb *CollaborationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cgb.build.ctx, "GroupBy")
	if err := cgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CollaborationQuery, *CollaborationGroupBy](ctx, cgb.build, cgb, cgb.build.inters, v)
}

func (cgb *CollaborationGroupBy) sqlScan(ctx context.Context, root *CollaborationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cgb.fns))
	for _, fn := range cgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cgb.flds)+len(cgb.fns))
		for _, f := range *cgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CollaborationSelect is the builder for selecting fields of Collaboration entities.
type CollaborationSelect struct {
	*CollaborationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cs *CollaborationSelect) Aggregate(fns ...AggregateFunc) *CollaborationSelect {
	cs.fns = append(cs.fns, fns...)
	return cs
}

// Scan applies the selector query and scans the result into the given value.
func (cs *CollaborationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cs.ctx, "Select")
	if err := cs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CollaborationQuery, *CollaborationSelect](ctx, cs.CollaborationQuery, cs, cs.inters, v)
}

func (cs *CollaborationSelect) sqlScan(ctx context.Context, root *CollaborationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cs.fns))
	for _, fn := range cs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
